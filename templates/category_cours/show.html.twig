{% extends 'base.html.twig' %}

{% block title %}CategoryCours{% endblock %}

{% block body %}
    <h1>CategoryCours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_cour.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_cour.name }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ category_cour.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_cours_index') }}">back to list</a>

    <a href="{{ path('category_cours_edit', {'id': category_cour.id}) }}">edit</a>

    {{ include('category_cours/_delete_form.html.twig') }}
{% endblock %}
