<form method="post" action="{{ path('category_cours_delete', {'id': category_cour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_cour.id) }}">
    <button class="btn">Delete</button>
</form>
